# 01_inventory

Machine-readable model inventory lives here (`inventory.csv`), generated from and kept
in sync with the human-readable `../MODEL_INVENTORY.md`. If they ever disagree,
`MODEL_INVENTORY.md` is authoritative — regenerate the CSV from it.

Also holds deduplication notes for the twice-present "RC-01 Revision 1 Files" tree.
